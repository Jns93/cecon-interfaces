export interface IInvoiceItem {
    // #region Properties (8)

    addition: number;
    description: string;
    discount: number;
    id: string;
    quantity: number;
    sku: string;
    totalPrice: number;
    unitPrice: number;

    // #endregion Properties (8)
}

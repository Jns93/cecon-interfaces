export interface ISubscriptionItem {
  // #region Properties (5)

  description: string;
  quantity: number;
  recurrent: boolean;
  totalPrice: number;
  unitPrice: number;

  // #endregion Properties (5)
}
